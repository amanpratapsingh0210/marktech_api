services:
  - type: web
    name: houseofmarktech-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: JWT_SECRET_KEY
        generateValue: true